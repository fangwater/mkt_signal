[package]
name = "mkt_parsers"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = { workspace = true }
bytes = { version = "1.0", features = ["serde"] }
log = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
symbol_utils = { path = "../symbol_utils" }
tokio = { version = "1.0", features = ["sync"] }
