[package]
name = "mkt_parsers"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = { workspace = true }
bytes = { version = "1.0", features = ["serde"] }
symbol_utils = { path = "../symbol_utils" }
serde_json = "1.0"
