[package]
name = "mkt_model_runtime"
version = "0.1.0"
edition = "2021"

[lib]
name = "mkt_model_runtime"
path = "src/lib.rs"

[dependencies]
anyhow = "1.0"
ort = { version = "2.0.0-rc.11", default-features = false, features = ["std", "ndarray"] }
