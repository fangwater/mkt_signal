[package]
name = "ipc_common"
version.workspace = true
edition.workspace = true

[dependencies]
anyhow.workspace = true
bytes = { version = "1.0", features = ["serde"] }
iceoryx2 = "0.8.1"
log = "0.4"
runtime_common = { path = "../runtime_common" }
signal_common = { path = "../signal_common" }
