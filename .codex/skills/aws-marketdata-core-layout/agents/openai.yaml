interface:
  display_name: "AWS Marketdata Core Layout"
  short_description: "Pin market-data cores and split dual-ENI traffic"
  default_prompt: "Use $aws-marketdata-core-layout to operate market-data publishers with the host's CPU and dual-ENI lane assignments."
