/target
node_modules
logs
monitor
*.log

# 批量服务器配置脚本（包含敏感信息）
batch_setup_servers.sh
batch_install_dev_tools.sh

# 本机一次性环境配置脚本（含 API key）
setup-codexzh-mac.sh
setup-codexzh-mac*.sh

# 仅本地排错用的 OKX 只读查询脚本（避免与生产脚本混用）
scripts/query_okx_account_snapshot.py
scripts/query_okx_order_history.py
scripts/query_okx_pending_orders.py

# OKX 下单时延测试（含 API key 调用，仅本地）
scripts/test_okx_order_latency.py

# Claude Code 本地配置（包含 ANTHROPIC_AUTH_TOKEN 等凭证）
.claude/settings.json

__pycache__/
.venv/
.venv

*.parquet
export_data
anlysis_binance_fr_trade/output/
anlysis_binance_fr_trade/*.csv

# factor_pool 产出文件
factor_pool/
factor_pool/**/*.ipynb
factor_pool/**/*.h5
.venv/**

# local model artifacts
model/

# SSH keys for FR remote deploy (never commit private keys)
*.pem
